global: {}

matrix:
  server:
    serverName: "ess-helm-production.up.railway.app"
    ingress:
      enabled: true
      host: "ess-helm-production.up.railway.app"
    synapse: {}   # keep defaults

elementWeb:
  enabled: true
  ingress:
    enabled: true
    host: "ess-helm-production.up.railway.app"

elementAdmin:
  enabled: true
  ingress:
    enabled: true
    host: "ess-helm-production.up.railway.app"

matrixAuthenticationService:
  enabled: true
  ingress:
    enabled: true
    host: "ess-helm-production.up.railway.app"

matrixRTC:
  enabled: true
  ingress:
    enabled: true
    host: "ess-helm-production.up.railway.app"

wellKnownDelegation:
  enabled: true   # still requires serverName, which we already set above