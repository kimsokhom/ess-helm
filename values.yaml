# 1. Global Server Name
serverName: "ess-helm-production.up.railway.app"

# 2. Synapse Configuration
synapse:
  ingress:
    host: "ess-helm-production.up.railway.app"

  postgres:
    host: "turntable.proxy.rlwy.net"
    port: 25787
    user: "postgres"
    # The password must be an object with a 'value' key
    password:
      value: "bfdyTYgtFgjWjSlZMLHZMKPMXpxGpcEe"
    database: "synapse_db"
    # sslmode removed as it is not supported in this schema block

# 3. Matrix Authentication Service (MAS)
matrixAuthenticationService:
  enabled: true
  ingress:
    host: "ess-helm-production.up.railway.app"

  postgres:
    host: "turntable.proxy.rlwy.net"
    port: 25787
    user: "postgres"
    # The password must be an object with a 'value' key
    password:
      value: "bfdyTYgtFgjWjSlZMLHZMKPMXpxGpcEe"
    database: "mas_db"
    # sslmode removed as it is not supported in this schema block

# 4. UI Components
elementWeb:
  enabled: true
  ingress:
    host: "ess-helm-production.up.railway.app"

elementAdmin:
  enabled: true
  ingress:
    host: "ess-helm-production.up.railway.app"

matrixRTC:
  enabled: true
  ingress:
    host: "ess-helm-production.up.railway.app"

# 5. Well-known delegation
wellKnownDelegation:
  enabled: true
